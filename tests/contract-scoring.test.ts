import assert from 'node:assert/strict';import test from 'node:test';import{evaluateContract}from'../src/utils/contract-scoring.js';
const base:any={id:'t',version:'1',tier:'easy',category:'tool_selection',description:'',prompt:'',tools:[],expectedToolCalls:[],difficulty:1,capabilities:[],scoring:{required:[{check:'tool_called',tool:'get_weather'},{check:'response_matches_regex',all_of:['Warsaw']}],forbidden:[{check:'call_count_at_most',n:3}],partial:[{check:'tool_args_contain',tool:'get_weather',args:{location:'Warsaw'}}]}};
test('evaluates required and partial contract checks',()=>{const r=evaluateContract(base,[{name:'get_weather',arguments:{location:'Warsaw'}}],'Warsaw weather',[]);assert.equal(r.pass,true);assert.equal(r.partial,1);assert.equal(r.forbiddenTriggered,0);});
test('detects forbidden checks',()=>{const scenario={...base,scoring:{required:[],forbidden:[{check:'tool_not_called',tool:'bad'}],partial:[]}};const r=evaluateContract(scenario,[{name:'bad',arguments:{}}],'',[]);assert.equal(r.pass,false);assert.equal(r.forbiddenTriggered,1);});
